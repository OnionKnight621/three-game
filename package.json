{
  "name": "three-game",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "git:add": "git add dist -f",
    "git:commit": "git commit -m \"add dist\"",
    "git:subtree": "git subtree push --prefix dist origin gh-pages"
  },
  "devDependencies": {
    "@types/three": "^0.143.0",
    "typescript": "^4.6.4",
    "vite": "^3.0.7"
  },
  "dependencies": {
    "three": "^0.143.0"
  }
}
